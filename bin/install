#!/usr/bin/env bash

set -euo pipefail

mkdir -p ${ASDF_INSTALL_PATH}/bin

# After the new install.sh is merged this can be replaced with ...
# curl -Lsf https://sh.benthos.dev | bash ${ASDF_INSTALL_VERSION} ${ASDF_INSTALL_PATH}/bin
./bin/install.sh ${ASDF_INSTALL_VERSION} ${ASDF_INSTALL_PATH}/bin
